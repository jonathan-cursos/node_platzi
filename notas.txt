Escalando la arquitectura: Múltiples entidades

-Vamos a agregar el componente user, hay 1 diferencia de como lo hicimos.
-Sí en el store retornamos la ejecución del metodo, estamos retornando una promesa, lo 
  que podemos hacer para capturar su error, es que, sí no hay un dato, retornemos 
  Promise.reject(), y así, ya en el network podemos usar un catch.
-Recordar agregar si instancia al routes.