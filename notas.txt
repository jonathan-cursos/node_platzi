Revisión y próximos pasos

-Una recomendación es habilitar las cabeceras CORS.
-El profesor indica que comenzando es bueno que las habilitamos todas, y conforme avanzamos
  aprendiendo, empecemos a ser reestrictivos con las cabeceras.
-Para ello, debemos instalar cors, e importarlo en el server.
-Lo añadimos con el app. cors, el modulo cors es un metodo.
-Esto es necesario para poder verlo desde un frontend.

-Para ello, descargamos la carpeta, la movemos a public, instalamos las dependencias, y
  ejecutamos el script serve.