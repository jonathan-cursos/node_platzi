MongoDB: Eliminar Datos

-En la función del store retornaremos el Model.deleteOne() e igual, tendrá el mismo filtro.
-Al ejecutar esta función en el controller, debemos manipularla como una promesa, porque
  es lo que devuelve deleteOne().
-En el then se ejecuta el resolve, y en el catch, el reject.